var TextUI = window.jsnamespace || {}

TextUI.DirGenerate = (
    function (element_id) {
        let text_elem = document.getElementById(element_id)
        if (text_elem != null) {
            
        }
    }
)
